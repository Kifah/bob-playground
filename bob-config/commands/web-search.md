Search the web using the Exa MCP tool and return well-structured results with sources.

Call the `web_search_exa` tool with the following query: $ARGUMENTS

Present the results clearly:
- Lead with a concise direct answer if the results support one.
- List the top sources with title, URL, and a one-sentence summary for each.
- If the query is ambiguous, note that and offer to refine.
